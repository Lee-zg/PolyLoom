# 贡献与历史代码准入

## 基本流程

1. 从 `main` 创建分支。
2. 新组件或插件通过 `pnpm generate` 进入 `experimental` 子路径。
3. 补齐实现、类型、测试、文档和元数据。
4. 执行全部验收命令。
5. 使用 `pnpm changeset` 记录面向使用者的变化。
6. 提交信息使用简洁中文，例如：`新增 React Button 加载状态`。

## 历史代码迁移

历史代码先放在 `packages/lab-vue` 或 `packages/lab-react`，不得直接复制进公开包。

迁移前确认：

- 代码、图片、图标、字体和依赖的许可证允许公开分发。
- 已移除域名、密钥、用户数据、内部接口、埋点和业务专用配置。
- 没有依赖原项目的全局变量、隐式 CSS 或构建别名。
- 浏览器 API 只在运行时需要时访问，模块本身可以在 SSR 环境导入。
- 组件具备键盘交互、焦点状态、语义元素与必要的 ARIA 属性。

## 从实验入口晋升

实验内容完成以下检查后，才能改为 `stable` 并进入模块根导出：

- 单元测试和 SSR 测试覆盖核心分支。
- 文档包含 API、示例、样式入口和限制。
- Vue 与 React 同名能力保持语义一致，同时保留框架习惯。
- `pnpm validate:exports`、`pnpm validate:packages` 与 `pnpm test:pack` 通过。
- 对公开 API 变化增加 Changeset。

## 代码约定

- 导出的函数、组件、类型应有简短用途说明。
- 注释解释业务原因、边界与权衡，不重复代码表面行为。
- 包导入时不得创建全局单例、注册监听器或访问 DOM。
- 框架运行时依赖必须 external，并优先声明为 peer dependency。
- JavaScript 不隐式加载 CSS；样式通过公开 CSS 子路径引入。
