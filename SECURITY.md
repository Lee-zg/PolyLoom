# 安全策略

## 支持范围

安全修复面向最新的次版本发布。`0.x` 阶段不会为更早版本长期维护独立补丁分支。

## 报告问题

请不要在公开 Issue 中披露尚未修复的漏洞、密钥或真实用户数据。仓库建立公开地址后，
通过 GitHub Security Advisory 私下提交报告。

报告应包含受影响包和版本、最小复现、可能影响以及已知缓解方式。维护者确认后会协调
修复、发布和公开披露时间。

## 组件安全边界

- 公开组件默认不渲染未经处理的 HTML。
- 插件不得在模块导入时读取存储、Cookie 或浏览器全局对象。
- 历史代码在许可证和敏感信息检查完成前只能进入私有实验包。
- npm 发布使用 OIDC Trusted Publishing，不在仓库中保存长期发布令牌。
