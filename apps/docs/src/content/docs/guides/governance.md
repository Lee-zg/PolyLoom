---
title: 历史代码准入
description: 从个人项目迁移组件时必须完成的检查。
---

历史组件不能直接进入公开导出。先放入对应的 `lab-*` 私有包，并补充来源元数据。

进入稳定包前必须满足：

1. 确认代码、图标、字体和图片的许可证允许公开再分发。
2. 删除业务域名、密钥、埋点、用户数据和内部接口。
3. 将框架、编辑器等宿主依赖声明为 peer dependency。
4. 保证导入时不访问浏览器全局对象，并通过 SSR 测试。
5. 补充类型、单元测试、键盘操作、无障碍检查和文档。
6. 增加 changeset，说明面向使用者的变化。
